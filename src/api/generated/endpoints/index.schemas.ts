/**
 * Generated by orval v8.21.0 🍺
 * Do not edit manually.
 * Stafy Time Tracking API
 * OpenAPI spec version: 1.0.0
 */
export interface DashboardTimeEntryOut {
  /** Time entry ID. */
  id: number;
  /** Entry start, ISO 8601 UTC. */
  time_start: string;
  /** Entry end, ISO 8601 UTC. */
  time_end: string;
  /** Entry date, ISO 8601 UTC — format on the client. */
  activity_date: string;
  /** Activity name. */
  activity_name: string;
  /** Duration in hours. */
  activity_hours: number;
  /**
     * Gross rate applied at entry creation.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  rate_hour: string;
  /**
     * Gross total for this entry.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  activity_total: string;
}

export interface DashboardUserInfoOut {
  /** Internal user ID. */
  id: number;
  /** User's first name. */
  first_name: string;
  /** User's last name. */
  last_name: string;
  /** User's email. */
  email: string;
  /** One of employee, manager, admin. */
  role: string;
}

/**
 * JSON error body shape emitted by app_http_exception_handler and
 * http_exception_handler. Attach via responses={code: {"model": ErrorOut, ...}}
 * so generated clients (Orval) get a typed error instead of `unknown`.
 */
export interface ErrorOut {
  code: string;
  detail: string;
}

export type ValidationErrorCtx = { [key: string]: unknown };

export interface ValidationError {
  loc: (string | number)[];
  msg: string;
  type: string;
  input?: unknown;
  ctx?: ValidationErrorCtx;
}

export interface HTTPValidationError {
  detail?: ValidationError[];
}

export interface HourlyRateOut {
  /** Activity ID. */
  activity_id: number;
  /** Activity name. */
  activity_name: string;
  /**
     * Gross hourly rate, serialized as a string.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  hourly_rate_gross: string;
}

export interface HourlyRateUpdate {
  /** Activity this rate applies to. */
  activity_id: number;
  /** New gross hourly rate. */
  hourly_rate_gross: number | string;
}

export interface HourlyRatesListOut {
  /** Hourly rates configured for the caller. */
  data: HourlyRateOut[];
}

export interface RootOut {
  /** API display name. */
  title: string;
  /** Currently live API version. */
  api_version: string;
  /** Human-readable health-check status. */
  status: string;
}

export interface TimeEntryActivityOut {
  /** Activity ID. */
  id: number;
  /** Activity name. */
  activity_name: string;
}

export interface TimeEntryIn {
  /** Entry start, ISO 8601 with UTC offset. */
  time_start: string;
  /** Entry end, ISO 8601 with UTC offset. */
  time_end: string;
  /** Activity this entry is logged against. */
  activity_id: number;
}

export interface TimeEntryOut {
  /** Time entry ID. */
  id: number;
  /** Entry start, ISO 8601 UTC. */
  time_start: string;
  /** Entry end, ISO 8601 UTC. */
  time_end: string;
  /**
     * Gross rate snapshot at creation time.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  rate_applied: string;
  /** Shallow embed of the activity. */
  activity: TimeEntryActivityOut;
}

export interface UserActivityCreate {
  /**
     * Name of the activity to create.
     * @minLength 2
     * @maxLength 100
     */
  activity_name: string;
  /** Gross hourly rate for this activity. */
  hourly_rate_gross: number | string;
}

/**
 * Gross rate per activity name.
 */
export type UserDashboardOutRatesMap = {[key: string]: string};

/**
 * Gross total per activity name this month.
 */
export type UserDashboardOutActivityGross = {[key: string]: string};

export interface UserDashboardOut {
  /** Shallow embed of the current user. */
  user_info: DashboardUserInfoOut;
  /** This month's time entries. */
  time_entries: DashboardTimeEntryOut[];
  /** Hourly rates configured for the caller. */
  hourly_rates: HourlyRateOut[];
  /** Total hours worked this month. */
  total_hours: number;
  /**
     * Total gross salary this month.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  total_gross_salary: string;
  /**
     * Average gross rate per hour this month.
     * @pattern ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
     */
  hourly_average: string;
  /** Gross rate per activity name. */
  rates_map: UserDashboardOutRatesMap;
  /** Gross total per activity name this month. */
  activity_gross: UserDashboardOutActivityGross;
}

export interface UserOut {
  /** Internal user ID. */
  id?: number | null;
  /** Firebase Auth UID. */
  firebase_uid?: string;
  /** User's first name. */
  first_name?: string | null;
  /** User's last name. */
  last_name?: string | null;
  /** User's email. */
  email?: string | null;
  /** Company the user belongs to. */
  company_id: number;
  /** Auth provider used to create this account. */
  auth_provider: string;
  /** Whether Firebase has verified the user's email. */
  email_verified?: boolean;
  /** One of employee, manager, admin. */
  role?: string | null;
  /** Account creation timestamp, UTC. */
  created_at?: string | null;
  /** Whether the account is active. */
  is_active?: boolean | null;
}

export interface UserRegisterIn {
  /**
     * First name.
     * @minLength 2
     * @maxLength 30
     */
  first_name: string;
  /**
     * Last name.
     * @minLength 2
     * @maxLength 30
     */
  last_name: string;
  /**
     * One of employee, manager, admin.
     * @pattern ^(employee|manager|admin)$
     */
  role: string;
}

export interface UsersListOut {
  /** Users in the caller's company. */
  data: UserOut[];
}

export type ValidationErrorOutFieldErrors = {[key: string]: string[]};

/**
 * 422 body shape emitted by validation_exception_handler.
 */
export interface ValidationErrorOut {
  code: string;
  detail: string;
  field_errors: ValidationErrorOutFieldErrors;
}

export interface VerifyTokenUserOut {
  /** Internal user ID. */
  id: number;
  /** User's first name. */
  first_name?: string | null;
  /** User's last name. */
  last_name?: string | null;
  /** User's email. */
  email?: string | null;
  /** User's role. */
  role?: string | null;
}

export interface VerifyTokenOut {
  /** Whether the provided Firebase token is valid. */
  valid: boolean;
  /** The authenticated user, shallow embed. */
  user: VerifyTokenUserOut;
}

